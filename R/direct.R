#Single Feature Predictor
#Tai-Hsien Ou Yang 
#04032013

single.train<-function ( X.train, Surv.train,  train.par ) {
	single=0
	cat("OK")
return(single)
}

single.predict<-function ( dummymodel, X.test, predict.par ) {
print(t(X.test))
return( as.numeric(t(X.test)))
}

